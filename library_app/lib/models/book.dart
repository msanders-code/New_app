// Defines a 'Book' class to store information for use later
class Book {
  final String title;
  final String author;
  final String cover;
  final String id;
  const Book(
      {required this.title,
      required this.author,
      required this.cover,
      required this.id});
}
